// next-sitemap.config.js
module.exports = {
    siteUrl: 'https://www.dandevcreator.com', // Replace with your site's URL
    generateRobotsTxt: true, // (optional) Generate robots.txt file
    // Exclude specific paths from the sitemap (optional)
    exclude: ['/api/*'],
  };
  