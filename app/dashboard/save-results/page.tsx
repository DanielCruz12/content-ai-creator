import React from "react";

const SaveResults = () => {
  console.log(
    "if communityData.bookmarks se encuentra el id del usuario que esta logeado.. entonces pinta solo esos"
  );
  return <div>SaveResults</div>;
};

export default SaveResults;
